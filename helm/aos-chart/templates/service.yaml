apiVersion: v1
kind: Service
metadata:
  name: {{ include "aos.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aos.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "aos.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
