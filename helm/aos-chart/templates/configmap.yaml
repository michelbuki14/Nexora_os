apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nexora.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nexora.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
