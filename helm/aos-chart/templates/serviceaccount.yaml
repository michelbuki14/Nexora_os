apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nexora.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "nexora.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
